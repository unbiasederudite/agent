# Copy to .env and fill in real values.
# litellm reads these automatically based on each model string's provider
# prefix in config.example.json (e.g. "anthropic/..." -> ANTHROPIC_API_KEY).
# Only set keys for the providers you actually list in your config.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
